# 数据库连接池耗尽问题说明

> **错误时间**: 2025-11-18 20:32  
> **错误信息**: `HikariPool-1 - Connection is not available, request timed out after 30014ms`

---

## 📋 错误详情

### **完整错误信息**

```
HikariPool-1 - Connection is not available, request timed out after 30014ms 
(total=10, active=10, idle=0, waiting=1)
```

### **参数解释**

| 参数 | 值 | 说明 |
|------|---|------|
| `total=10` | 10 | **连接池总共 10 个连接** |
| `active=10` | 10 | **10 个连接全部被占用** ❌ |
| `idle=0` | 0 | **0 个空闲连接** ❌ |
| `waiting=1` | 1 | **1 个请求在等待连接** ⚠️ |
| `timeout` | 30 秒 | **等待 30 秒后超时** |

---

## 🔍 问题分析

### **根本原因：数据库连接池太小**

**默认配置**：
```properties
# HikariCP 默认最大连接数
spring.datasource.hikari.maximum-pool-size=10  # 默认值
```

**问题**：
- ✅ 10 个连接对于普通应用足够
- ❌ 但你的应用有 **SSE 长连接**，每个 SSE 连接可能占用数据库连接
- ❌ 当有多个用户同时在线时，连接池很快耗尽

---

### **为什么 SSE 会占用数据库连接？**

#### **场景1: SSE 连接建立时**

```java
// NotificationSseController.java
@GetMapping(value = "/stream", produces = MediaType.TEXT_EVENT_STREAM_VALUE)
public ResponseEntity<SseEmitter> streamNotifications(HttpServletRequest request) {
    Integer userId = (Integer) request.getAttribute("userId");
    
    // 1. 查询用户信息（占用数据库连接）
    Employee emp = employeeRepository.findById(userId).orElse(null);
    
    // 2. 创建 SSE 连接（持续 30 分钟）
    SseEmitter emitter = new SseEmitter(30 * 60 * 1000L);
    
    // 3. 推送待接收通知（占用数据库连接）
    pushService.registerConnection(userId, emp.getEmployeeName(), emitter);
    
    // 4. 异步更新通知状态（占用数据库连接）
    new Thread(() -> statusUpdater.updatePendingNoticesStatus(userId)).start();
    
    return ResponseEntity.ok(emitter);
}
```

**问题**：
- ❌ 步骤 4 创建了新线程，但**没有正确管理事务**
- ❌ 如果事务没有提交，数据库连接会一直被占用
- ❌ SSE 连接持续 30 分钟，连接可能被长时间占用

---

#### **场景2: 定时心跳检测**

```java
// NotificationPushService.java
@Scheduled(fixedRate = 30000)  // 每 30 秒执行一次
public void sendHeartbeat() {
    for (Map.Entry<Integer, SseEmitter> entry : emitters.entrySet()) {
        try {
            entry.getValue().send(SseEmitter.event()
                .name("heartbeat")
                .data("ping"));
        } catch (IOException e) {
            // 连接断开，移除
            emitters.remove(entry.getKey());
        }
    }
}
```

**这个不会占用数据库连接**，只是发送心跳。

---

### **连接占用情况分析**

**假设场景**：10 个用户同时在线

```
用户1 建立 SSE 连接 → 查询用户信息（占用连接1）→ 更新通知状态（占用连接2）
用户2 建立 SSE 连接 → 查询用户信息（占用连接3）→ 更新通知状态（占用连接4）
用户3 建立 SSE 连接 → 查询用户信息（占用连接5）→ 更新通知状态（占用连接6）
用户4 建立 SSE 连接 → 查询用户信息（占用连接7）→ 更新通知状态（占用连接8）
用户5 建立 SSE 连接 → 查询用户信息（占用连接9）→ 更新通知状态（占用连接10）

此时：
- 所有 10 个连接都被占用 ❌
- 用户6 尝试建立 SSE 连接 → 等待连接 → 30 秒后超时 ❌
```

---

## ✅ 解决方案

### **方案1: 增加数据库连接池大小（已实施）⭐⭐⭐⭐⭐**

**修改 `application.properties`**：

```properties
# ===== HikariCP 数据库连接池配置 =====
# 最大连接数（默认 10）
# SSE 长连接和普通请求都需要数据库连接，需要增加连接池大小
spring.datasource.hikari.maximum-pool-size=50

# 最小空闲连接数（默认与 maximum-pool-size 相同）
spring.datasource.hikari.minimum-idle=10

# 连接超时时间（毫秒，默认 30000）
spring.datasource.hikari.connection-timeout=30000

# 空闲连接最大存活时间（毫秒，默认 600000 = 10分钟）
spring.datasource.hikari.idle-timeout=600000

# 连接最大存活时间（毫秒，默认 1800000 = 30分钟）
spring.datasource.hikari.max-lifetime=1800000
```

**效果**：
- ✅ 最多支持 50 个并发数据库连接
- ✅ 即使有 20 个 SSE 连接，还有 30 个连接处理其他请求
- ✅ 最小保持 10 个空闲连接

---

### **方案2: 确保事务正确提交（已实施）⭐⭐⭐⭐⭐**

**问题代码**：

```java
// NotificationStatusUpdater.java
public void updatePendingNoticesStatus(Integer userId) {
    // 查询数据库
    List<NoticeEmployee> notReceivedNotices = noticeEmployeeRepository
        .findByIdReceiverIdAndNoticeStatus(userId, NoticeStatus.NOT_RECEIVED);
    
    // 更新数据库
    noticeEmployeeRepository.saveAll(notReceivedNotices);
    
    // ❌ 没有 @Transactional 注解，事务可能不会自动提交
    // ❌ 数据库连接可能不会释放
}
```

**修复后**：

```java
// NotificationStatusUpdater.java
@Transactional  // ✅ 添加事务注解
public void updatePendingNoticesStatus(Integer userId) {
    // 查询数据库
    List<NoticeEmployee> notReceivedNotices = noticeEmployeeRepository
        .findByIdReceiverIdAndNoticeStatus(userId, NoticeStatus.NOT_RECEIVED);
    
    // 更新数据库
    noticeEmployeeRepository.saveAll(notReceivedNotices);
    
    // ✅ 方法结束时，事务自动提交，连接自动释放
}
```

**效果**：
- ✅ 确保事务正确提交
- ✅ 数据库连接及时释放
- ✅ 避免连接泄漏

---

### **方案3: 优化 SSE 连接建立流程（推荐）⭐⭐⭐⭐**

**当前问题**：

```java
// NotificationSseController.java
@GetMapping(value = "/stream")
public ResponseEntity<SseEmitter> streamNotifications(HttpServletRequest request) {
    Integer userId = (Integer) request.getAttribute("userId");
    
    // 1. 查询用户（占用连接）
    Employee emp = employeeRepository.findById(userId).orElse(null);
    
    // 2. 创建 SSE 连接
    SseEmitter emitter = new SseEmitter(30 * 60 * 1000L);
    
    // 3. 推送通知（占用连接）
    pushService.registerConnection(userId, emp.getEmployeeName(), emitter);
    
    // 4. 异步更新状态（占用连接）❌ 问题：新线程中的事务管理
    new Thread(() -> statusUpdater.updatePendingNoticesStatus(userId)).start();
    
    return ResponseEntity.ok(emitter);
}
```

**优化方案**：使用 Spring 的异步任务

```java
// NotificationSseController.java
@GetMapping(value = "/stream")
public ResponseEntity<SseEmitter> streamNotifications(HttpServletRequest request) {
    Integer userId = (Integer) request.getAttribute("userId");
    
    // 1. 查询用户（占用连接，但会立即释放）
    Employee emp = employeeRepository.findById(userId).orElse(null);
    
    // 2. 创建 SSE 连接
    SseEmitter emitter = new SseEmitter(30 * 60 * 1000L);
    
    // 3. 推送通知（占用连接，但会立即释放）
    pushService.registerConnection(userId, emp.getEmployeeName(), emitter);
    
    // 4. 使用 Spring 异步任务（正确管理事务）✅
    asyncTaskExecutor.execute(() -> {
        statusUpdater.updatePendingNoticesStatus(userId);
    });
    
    return ResponseEntity.ok(emitter);
}
```

**需要配置异步任务执行器**：

```java
@Configuration
@EnableAsync
public class AsyncConfig {
    
    @Bean(name = "asyncTaskExecutor")
    public Executor asyncTaskExecutor() {
        ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
        executor.setCorePoolSize(10);
        executor.setMaxPoolSize(20);
        executor.setQueueCapacity(100);
        executor.setThreadNamePrefix("async-");
        executor.initialize();
        return executor;
    }
}
```

---

### **方案4: 使用 @Async 注解（推荐）⭐⭐⭐⭐**

**修改 `NotificationStatusUpdater.java`**：

```java
@Service
public class NotificationStatusUpdater {
    
    @Async  // ✅ 使用 Spring 的异步注解
    @Transactional  // ✅ 确保事务正确管理
    public void updatePendingNoticesStatus(Integer userId) {
        // 查询和更新数据库
        // ...
    }
}
```

**修改 `NotificationSseController.java`**：

```java
@GetMapping(value = "/stream")
public ResponseEntity<SseEmitter> streamNotifications(HttpServletRequest request) {
    Integer userId = (Integer) request.getAttribute("userId");
    
    Employee emp = employeeRepository.findById(userId).orElse(null);
    SseEmitter emitter = new SseEmitter(30 * 60 * 1000L);
    
    pushService.registerConnection(userId, emp.getEmployeeName(), emitter);
    
    // ✅ 直接调用，Spring 会异步执行
    statusUpdater.updatePendingNoticesStatus(userId);
    
    return ResponseEntity.ok(emitter);
}
```

**需要启用异步支持**：

```java
@SpringBootApplication
@EnableAsync  // ✅ 启用异步支持
public class BackendApplication {
    public static void main(String[] args) {
        SpringApplication.run(BackendApplication.class, args);
    }
}
```

---

## 📊 各方案对比

| 方案 | 难度 | 效果 | 推荐度 | 状态 |
|------|------|------|--------|------|
| **方案1: 增加连接池** | ⭐ 简单 | ⭐⭐⭐⭐ 好 | ⭐⭐⭐⭐⭐ | ✅ 已实施 |
| **方案2: 添加 @Transactional** | ⭐ 简单 | ⭐⭐⭐⭐⭐ 最好 | ⭐⭐⭐⭐⭐ | ✅ 已实施 |
| **方案3: 使用异步任务执行器** | ⭐⭐ 中等 | ⭐⭐⭐⭐ 好 | ⭐⭐⭐⭐ | ⚠️ 推荐实施 |
| **方案4: 使用 @Async** | ⭐ 简单 | ⭐⭐⭐⭐⭐ 最好 | ⭐⭐⭐⭐⭐ | ⚠️ 推荐实施 |

---

## 🎯 推荐实施步骤

### **立即实施（已完成）✅**

1. ✅ **增加数据库连接池大小**
   - 修改 `application.properties`
   - `maximum-pool-size=50`

2. ✅ **添加 @Transactional 注解**
   - 修改 `NotificationStatusUpdater.java`
   - 确保事务正确提交

### **短期优化（推荐）**

3. **使用 @Async 注解**
   - 在 `NotificationStatusUpdater.updatePendingNoticesStatus()` 添加 `@Async`
   - 在主类添加 `@EnableAsync`
   - 配置异步任务执行器

4. **添加数据库连接监控**
   - 使用 Spring Actuator 监控连接池状态
   - 实时查看 `active`、`idle`、`waiting` 数量

---

## 📈 监控数据库连接池

### **添加 Actuator 监控**

**1. 确保已添加依赖**：

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-actuator</artifactId>
</dependency>
```

**2. 启用 HikariCP 监控**：

```properties
# application.properties
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
management.metrics.enable.hikaricp=true
```

**3. 查看连接池状态**：

```bash
# 查看 HikariCP 指标
curl http://localhost:8080/actuator/metrics/hikaricp.connections.active
curl http://localhost:8080/actuator/metrics/hikaricp.connections.idle
curl http://localhost:8080/actuator/metrics/hikaricp.connections
curl http://localhost:8080/actuator/metrics/hikaricp.connections.pending
```

---

### **自定义监控端点**

```java
@RestController
@RequestMapping("/admin")
public class MonitorController {
    
    @Autowired
    private DataSource dataSource;
    
    @GetMapping("/db-pool-status")
    public Map<String, Object> getDbPoolStatus() {
        HikariDataSource hikariDataSource = (HikariDataSource) dataSource;
        HikariPoolMXBean poolMXBean = hikariDataSource.getHikariPoolMXBean();
        
        Map<String, Object> status = new HashMap<>();
        status.put("activeConnections", poolMXBean.getActiveConnections());
        status.put("idleConnections", poolMXBean.getIdleConnections());
        status.put("totalConnections", poolMXBean.getTotalConnections());
        status.put("threadsAwaitingConnection", poolMXBean.getThreadsAwaitingConnection());
        
        return status;
    }
}
```

---

## 🚨 告警阈值建议

| 指标 | 正常 | 警告 | 危险 |
|------|------|------|------|
| **活跃连接数** | < 30 | 30-40 | > 40 |
| **空闲连接数** | > 10 | 5-10 | < 5 |
| **等待连接数** | 0 | 1-3 | > 3 |
| **连接池使用率** | < 60% | 60%-80% | > 80% |

---

## 📝 总结

### **问题原因**

1. **数据库连接池太小**（默认 10 个）
2. **事务没有正确提交**（缺少 `@Transactional`）
3. **异步任务管理不当**（使用 `new Thread()` 而不是 Spring 异步）

### **解决方案**

1. ✅ **已完成**：增加连接池到 50
2. ✅ **已完成**：添加 `@Transactional` 注解
3. ⚠️ **推荐**：使用 `@Async` 注解
4. ⚠️ **推荐**：添加连接池监控

### **预期效果**

- ✅ 支持 50 个并发数据库连接
- ✅ 事务正确提交，连接及时释放
- ✅ 不会再出现连接池耗尽的问题

---

**文档版本**: v1.0  
**最后更新**: 2025-11-18  
**状态**: 已解决（需重启服务器）
